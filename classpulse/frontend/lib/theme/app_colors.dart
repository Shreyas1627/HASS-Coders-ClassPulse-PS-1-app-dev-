import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFF3B82F6);
  static const Color primaryDark = Color(0xFF1D4ED8);

  // Secondary / green accent
  static const Color secondary = Color(0xFF10B981);
  static const Color secondaryLight = Color(0xFF34D399);

  // Background — crisp, flat
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);

  // PIN
  static const Color pinBackground = Color(0xFFF1F5F9);
  static const Color pinBorder = Color(0xFFCBD5E1);
  static const Color pinActiveBorder = Color(0xFF2563EB);

  // Numpad
  static const Color numpadButton = Color(0xFFE2E8F0);
  static const Color numpadText = Color(0xFF1E293B);

  // Join button
  static const Color buttonDisabled = Color(0xFFCBD5E1);
  static const Color buttonDisabledText = Color(0xFF94A3B8);
  static const Color buttonEnabled = Color(0xFF2563EB);

  // Scanner
  static const Color scannerSurface = Color(0xFFF1F5F9);

  // Dashboard — Warning / Error
  static const Color warning = Color(0xFFEF4444);
  static const Color warningLight = Color(0xFFFEE2E2);
  static const Color warningBorder = Color(0xFFFCA5A5);

  // Dashboard — Success
  static const Color success = Color(0xFF10B981);
  static const Color successLight = Color(0xFFD1FAE5);

  // Dashboard — Amber accent
  static const Color amber = Color(0xFFF59E0B);
  static const Color amberLight = Color(0xFFFEF3C7);

  // Dashboard — Neutral UI
  static const Color divider = Color(0xFFE2E8F0);
  static const Color cardShadow = Color(0x0A000000);
  static const Color surfaceAlt = Color(0xFFF1F5F9);

  // Bottom Navigation
  static const Color navBarBackground = Color(0xFFFFFFFF);
  static const Color navBarActive = Color(0xFF2563EB);
  static const Color navBarInactive = Color(0xFF94A3B8);

  // Avatars
  static const Color avatarDefault = Color(0xFF6366F1);
}
